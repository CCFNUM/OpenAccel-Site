/**
 * Gallery has been removed. This component exists only to satisfy any
 * leftover imports. App.tsx redirects /gallery → /tutorials via GalleryRedirect.
 */
export function Gallery() {
  return null;
}
